# 赋值运算符
# “=、+=、-=、*=、/=、%=、**=、//=、:=”

# todo: 重点不同
#  :=  海象运算符
# 详细解释一下:=海象运算符
"""
    基本概念
    名称：海象运算符（Walrus Operator）
    引入版本：Python 3.8+
    符号：:=
    特点：同时进行赋值和返回操作的运算符
    主要用途
    在表达式中赋值并使用
    允许在条件表达式或其他表达式中同时赋值和检查变量
    避免重复计算相同的表达式
    简化代码结构
    减少代码行数
    提高代码可读性（在适当情况下）
"""
print("-------------使用传统写法00------------")
age = int(input("请输入您的年龄："))
if age >=18:
    print("可以上网")
else:
    print("不可以上网")
print("-------------使用海象运算符01------------")
if (n := len(input("请输入数据："))) > 2:
    print(f"数据长度为：{n}")

print("-------------使用海象运算符02------------")
  # 读取文件直到遇到空行
  # while (line := file.readline()) != "":
  #     process(line)
print("-------------使用海象运算符03------------")
# 避免重复调用 expensive_function
  #results = [y for x in data if (y := expensive_function(x)) > 0]
'''
    注意事项
    不要过度使用，以免降低代码可读性
    主要适用于需要在表达式中同时赋值和使用的场景
    在复杂的表达式中使用时要确保逻辑清晰
'''